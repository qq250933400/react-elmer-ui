import { Model } from "@MSJApp";
import { TypeModel } from "./index";
import { TypeApi, TypeApiEvent } from "../Api";

export default class Base extends Model<TypeModel, TypeApi, TypeApiEvent> {
    // the basic model
}