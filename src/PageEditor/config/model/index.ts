import Menu from "./Menu";
import Portal from "./Portal";
import App from "./App";

export type TypeModels = {
    menu: Menu;
    portal: Portal;
    app: App;
};

export const UseModel = {
    menu: Menu,
    portal: Portal,
    app: App
};